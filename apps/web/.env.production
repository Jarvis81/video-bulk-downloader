# Production/export build is served same-origin by Fastify inside Electron,
# so the UI calls the API with relative URLs (empty base). Web dev (next dev)
# ignores this file and uses the default http://127.0.0.1:4319.
NEXT_PUBLIC_API_BASE=
